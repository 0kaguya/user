# Zed config

+ This folder contains parts of the user space zed config file, which is
  located at `.config/zed/settings.json`.

+ Schemas are at `https://raw.githubusercontent.com/zed-industries/zed/refs/heads/main/assets/settings/default.json`.

+ AI-written lines should be placed into `50-llm-gen.json`.

+ Config should follows the described goals in README.md.
